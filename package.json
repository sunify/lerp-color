{
  "name": "@sunify/lerp-color",
  "version": "1.1.0",
  "main": "dist/index.js",
  "license": "MIT",
  "repository": "git@github.com:sunify/lerp-color.git",
  "author": "Alex Lunyov <isuntc@gmail.com>",
  "dependencies": {
    "@types/hex-rgb": "^3.0.0",
    "@types/rgb-hex": "^2.1.0",
    "hex-rgb": "^3.0.0",
    "lerp-array": "^1.1.1",
    "rgb-hex": "^2.1.0"
  },
  "devDependencies": {
    "@types/jest": "^23.3.13",
    "jest": "^24.0.0",
    "parcel-bundler": "^1.11.0",
    "size-limit": "^0.21.1",
    "ts-jest": "^23.10.5",
    "typescript": "^3.2.4"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "prebuild": "rm -rf dist && mkdir dist",
    "build": "yarn prebuild && tsc && yarn size",
    "prepublish": "yarn build",
    "size": "size-limit",
    "start-demo": "parcel index.html --open --out-dir=demo",
    "build-demo": "parcel build --out-dir=demo --public-url '.' index.html"
  }
}
